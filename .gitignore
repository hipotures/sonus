# General
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*.log
tmp/
temp/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.eggs/
dist/
build/
develop-eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg
MANIFEST

# Testing & Linting
.tox/
.mypy_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
test-results.xml
*.spec

# Terraform / OpenTofu
# Local .terraform directories
**/.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore plan files
*.tfplan

# Google Cloud Platform
*.key.json
gcp-sa-key.json
credentials.json
# Note: Be careful not to ignore legitimate config JSONs, so we don't do *.json

# Environment Variables
.env
.env.*
!.env.example

# Project Specific Output / Scratchpads
terraform_files.txt
examples/tuning_results/
examples/*.err
examples/nonexistent.err
examples/tmp/

# Sonus specific temporary files
*.tmp
*.err

# Gemini / Agent Context
.gemini/
